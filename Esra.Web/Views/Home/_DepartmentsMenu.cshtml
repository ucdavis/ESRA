<div id="departments-menu">
    <dl>
        <dt>
            @Html.ActionLink("Salary Scales", "Index", "SalaryScale")
        </dt>
        <dd>
            View Titles and Their Respective Salary Scales.</dd>
        <dt>
            @Html.ActionLink("Employee Salary Comparison", "Index", "EmployeeSalaryComparison")
        </dt>
        <dd>
            Compare Employees' Salaries By Title.</dd>
        <dt>
            @Html.ActionLink("Salary Review Analysis", "Index", "SalaryReviewAnalysis")
        </dt>
        <dd>
            Create a New Salary Review Analysis or Search For and View an Existing Salary Review
            Analysis.</dd>
    </dl>
    <div id="div-emulate-user" @if (User.IsInRole("EmulationUser") == false)
                               { <text>style="display: none;"</text> }
                                 >
        @using (Html.BeginForm("Emulate", "Account", FormMethod.Post))
        {
            @Html.TextBox("id")

            <input id="btnEmulateUserSubmit" type="submit" value="Emulate" />
        }
    </div>
</div>